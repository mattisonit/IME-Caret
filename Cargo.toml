[package]
name = "ime-caret"
version = "2.3.0"
edition = "2021"
rust-version = "1.70"
authors = ["OpenAI"]
description = "Subtle Windows IME state indicator attached to the active editable text caret"
license = "MIT"

[dependencies]

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"
